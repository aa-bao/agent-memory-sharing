# 复制本文件为 .env，供 scripts/start-ov.sh / start-ov.cmd 在没有全局环境变量时回退读取。
#
# ⚠️ 等号两侧不要有空格！
#    批处理的 `for /f "tokens=1,* delims=="` 会保留前导空格，把 key 弄坏。
#
# 推荐做法仍然是设成用户级环境变量（这样计划任务起的进程也能读到）：
#     setx SILICONFLOW_KEY "sk-xxxx"
#
# .env 已在 .gitignore 里，不会被提交。

SILICONFLOW_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
